import { StyleSheet } from "react-native"

export const requestLicenseUpdateStyle = StyleSheet.create({ 
  headerTitle: {
    fontSize: 16,
    textAlign:'center',
    marginBottom: 15
  },
  headerDescription: {
    fontSize: 12,
    textAlign:'center'
  }
})